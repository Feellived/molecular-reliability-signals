# 판을 고정한다. 지문 모델이 joblib으로 저장되어 있어 scikit-learn과 xgboost
# 판이 다르면 역직렬화가 실패하거나 다른 결과를 낸다. numpy와 pandas는 그
# 피클 안에 배열 형식이 들어가므로 함께 묶는다.
#
# 판을 올릴 때는 리눅스 x86_64 휠이 실재하는지 먼저 확인한다. 로컬에 설치된
# 판이 PyPI에 없는 경우가 있다.
fastapi==0.141.1
uvicorn[standard]==0.52.4
numpy==2.4.6
pandas==3.0.5
scikit-learn==1.9.0
xgboost==3.2.0
joblib==1.5.3
rdkit==2025.9.6
dimorphite-dl==2.0.2
torch==2.13.0
transformers==5.16.1
