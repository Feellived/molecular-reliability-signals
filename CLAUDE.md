# 프로젝트: 분자 물성 예측의 신뢰성 신호 평가

## 한 줄 요약

기존 신뢰성 지표(적용가능도메인, 컨포멀 불확실성)는 전부 모델 쪽(학습 데이터 분포, 모델 출력)에서 나온다. 이 프로젝트는 입력 쪽, 즉 같은 화합물을 컴퓨터에 넣을 때 표기·화학적 상태가 여러 가지일 수 있다는 사실에서 나오는 신호(표현 불안정성 A, 입력 상태 민감성 B)를 정량화하고, 기존 지표 대비 추가 가치가 있는지 골격 기반 분할 조건에서 검증한다.

- 팀: TOBIG's 동아리, 4인
- 기간: 2026-08 착수 ~ 2026-11 발표, 약 13주
- 성격: 새 예측 모델을 만드는 연구가 아니라, 기존 예측 모델의 출력에 대한 신뢰성 감사(audit) 연구

## 핵심 설계 원칙 (반드시 지킬 것)

1. **여러 신호를 하나로 합치는 것 자체를 기여로 내세우지 않는다.** QUAD(2026)가 이미 시도했고 결과는 부정적이었다(`Journal/QUAD-*.pdf`). 이 프로젝트의 기여는 통합이 아니라, 발생 원천이 다른 새 신호(입력 상태 민감성) 하나를 처음으로 분자 단위로 정량화하고 검증하는 것이다.
2. **1차 가설은 B축(입력 상태 민감성) 중심으로 좁힌다.** A축(표현 불안정성)은 Maxsmi(2021)가 이미 다룬 영역이라 참신성이 약하고, SMILES 언어모델이 있어야만 0이 아닌 값이 나오는 성립 조건이 있다. A가 흔들려도 프로젝트 핵심 주장은 B로 버틴다.
3. **분할 순서를 어기지 않는다.** 부모 분자 확정 → 중복 제거 → 골격 기반 분할 → 그 다음에 변형(등가 SMILES, 호변이성질체, 염, 입체) 생성. 순서를 바꾸면 누출이 생겨 전체 결과가 무효화된다.
4. **1주차 착수 게이트를 반드시 통과한 뒤 본 작업에 들어간다.** 사용할 데이터셋에서 호변이성질체·염 형태·입체 표기가 실제로 몇 개나 변형 가능한지 세어보고, 10퍼센트 미만인 축은 그 자리에서 버린다. 특히 B2(염 형태)·B3(입체 표기)는 소멸 가능성이 실재한다.
5. **표현 불안정성(A)을 측정하려면 SMILES를 직접 읽는 모델이 필수다.** 분자 지문(Morgan fingerprint) 기반 모델에서는 A가 원리상 항상 0이다.

## 작업 환경

이 폴더는 Google Drive에 있고 Colab에서 마운트해서 쓴다. 그래서 코드(`scripts/`)·데이터(`data/`)·문서는 이 폴더 안에 두어 Colab이 그대로 읽을 수 있게 한다.

로컬(맥)에서 CLI로 작업할 때 쓰는 Python 가상환경(`.venv`)은 Drive 폴더 밖(`~/.venvs/molecular-reliability-signals`)에 둔다. 가상환경은 파일 수가 많아 Drive 동기화를 심하게 느리게 만들기 때문이다. Colab에서는 이 로컬 venv를 쓰지 않고 노트북 안에서 그때그때 `pip install`한다. 로컬에서 스크립트를 실행할 때는 다음으로 활성화한다.

```bash
source ~/.venvs/molecular-reliability-signals/bin/activate
```

## 문서·자료 구조

- `Journal/` — 검증된 선행연구 원문 PDF 모음. 인용 전에는 항상 이 폴더의 원문을 우선 확인한다. DOI를 만들어내지 않는다.
- `docs/연구계획서.md` — 연구 본문. 등록 보고서(registered report) 형식으로 서론, 관련 연구, 문제 정식화, 연구 방법, 평가 프로토콜, 예상 결과, 논의, 결론, 참고문헌, 용어 해설 부록을 담는다. 세부 설계는 이 파일을 기준으로 삼는다. 이 CLAUDE.md는 방향만 잡는 문서이며 세부 설계를 대체하지 않는다.
- `docs/실행계획.md` — 프로젝트 운영 문서. 13주 일정, 역할 분담, 사전 선별 판정 절차, 실행 리스크, 발표 대응, 사전 학습 안내. 연구 내용은 담지 않는다.
- `docs/역할분담_피드백.md` — 실제 팀 구성(실력자 2, 비실력자 2)에 맞춘 역할 분담 조정안. 실행계획.md 3장의 역할표를 대체하지 않고 소유 방식만 보완한다.
- `docs/문헌_정리.md` — `Journal/`의 11편 논문 각각의 핵심 내용과 프로젝트 적용 지점.
- `data/raw/` — TDC ADMET Benchmark Group 22종 + MoleculeNet ESOL 파일럿. `scripts/download_admet.py`, `scripts/download_esol_pilot.py`로 재현 가능하므로 git으로 추적하지 않는다.

## 용어·문체 규칙

- 화학정보학 표준 용어만 쓴다. 임의로 만든 표현 금지. 불가피하게 비표준 표현을 써야 하면 반드시 풀어서 설명한다.
- 한국어 문어체로 작성한다. 팀 공유 문서이므로 도메인 배경이 없는 팀원도 읽을 수 있게 쉽게 쓰되, 구어체나 이모지는 쓰지 않는다.
- 마크다운은 노션에 붙여넣기 가능한 형태로 유지한다. 굵게 표시(`**`)를 렌더링되지 않는 상태로 남기지 않는다. 최상위 제목은 Heading 2(`##`)로 시작한다.

## 역할 분담 (요지)

- 담당 1: 데이터·화학 공간·적용가능도메인
- 담당 2: 예측 모델(지문 기반 + SMILES 모델)·컨포멀 불확실성
- 담당 3: 변형 생성(등가 SMILES, 호변이성질체, 양성자화, 염, 입체)·분산 분해(A, B1/B2/B3)
- 담당 4: 통합·평가·분할 규율 감사(PM 겸임)

## 하지 않는 것

- 22종 물성 전체를 동일한 깊이로 다루는 것 (사전 지정 4~6종만 심층 분석)
- 짝지은 분자쌍(MMP) 기반 반사실 검증
- 여러 신호를 단일 점수로 합쳐 제시하는 것
- 무작위 분할 결과를 최종 결론의 근거로 쓰는 것 (예비 확인용으로만)
